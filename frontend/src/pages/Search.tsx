import { CommunitySearch } from './Community';

// FR-1.2: community search route. Renders the partial-match community search
// experience (the reusable <CommunitySearch /> defined in Community.tsx).
export default function Search() {
  return <CommunitySearch />;
}
